  

function Student(props){
    return(
        <div>
            <h1>The name of the student is {props.name} and age is {props.age} and dob is {props.dob} and course is {props.course}</h1>
        </div>
    )
}
export default Student;